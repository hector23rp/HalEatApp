<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_height"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_screen"
    android:weightSum="100"
    tools:context="com.haleat.haleat.WeightActivity">

    <TextView
        android:id="@+id/weightTitle"
        style="@style/TextTitle"
        android:layout_marginTop="30dp"
        android:text="@string/weightTitle"/>

    <Button
        android:id="@+id/arrowWeightUp"
        style="@style/ButtonArrow"
        android:layout_marginLeft="150dp"
        android:layout_marginTop="150dp"
        android:text="Up"/>

    <TextView
        android:id="@+id/weightNumber"
        style="@style/BirthDayContent"
        android:text="85"/>

    <Button
        android:id="@+id/arrowWeightDown"
        style="@style/ButtonArrow"
        android:layout_marginLeft="150dp"
        android:layout_marginTop="320dp"
        android:text="Down"/>

    <LinearLayout
        android:id="@+id/layoutDots"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dots_height"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/dots_margin_bottom"
        android:gravity="center"
        android:orientation="horizontal"></LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:alpha=".5"
        android:layout_above="@id/layoutDots"
        android:background="@android:color/white" />

    <Button
        android:id="@+id/btn_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:background="@null"
        android:text="@string/back"
        android:textColor="@android:color/white" />

    <Button
        android:id="@+id/nextWeight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@null"
        android:text="@string/next"
        android:textColor="@android:color/white" />

</RelativeLayout>
